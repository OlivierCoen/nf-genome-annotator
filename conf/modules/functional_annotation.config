process {

    withName: 'EGGNOGMAPPER_EMAPPER' {
        ext.args   = { [
            "-m", "diamond",
            "--sensmode", "ultra-sensitive",
            "--dmnd_iterate", "yes"
        ].join(' ').trim() }
    }

}
